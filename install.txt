npm install
npm install typings --global
typings install dt~jquery --save --global

use "npm start" in project folder to start server  

Сборка для прода

"webpack" из корня папки. Важно помнить, что для сборки у нас отдельный index.html (index_webpack.html). Правки необходимо дублировать 